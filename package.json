{
  "name": "graphy_server",
  "version": "1.0.0",
  "description": "Express GraphQL server for US cities",
  "main": "graphserver.js",
  "scripts": {
    "start": "node graphserver.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "express": "^4.18.1",
    "express-graphql": "^0.12.0",
    "graphql": "^15.8.0"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
